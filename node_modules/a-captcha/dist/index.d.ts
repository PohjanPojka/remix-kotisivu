/// <reference types="node" />
import { WIDTH, HEIGHT } from "./constants";
export { WIDTH, HEIGHT };
export interface Options {
    length?: number;
    useLine?: boolean;
    useBlur?: boolean;
    useHollow?: boolean;
    dots?: number;
    fgColor?: number[];
    bgColor?: number[];
    sw: number;
}
export interface CaptchaData {
    text: string;
    buffer: Buffer;
}
export declare function captcha(options?: Options): Promise<CaptchaData>;
export declare function randomColor(start?: number, end?: number, opacity?: number): number[];
